const userName = prompt("Enter the student name");

    if (userName) {
      document.getElementById("ak").innerText = "Welcome to ICIT INSTITUTE online registration from, " + userName + "!";
    }